name = "cultural-committee-dashboard"
compatibility_date = "2025-09-24"

# Static site only — do not upload dev dependencies (see .assetsignore).
[assets]
directory = "."
not_found_handling = "single-page-application"
